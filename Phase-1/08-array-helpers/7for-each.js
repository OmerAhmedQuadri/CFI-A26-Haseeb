//for each

const arr = [0, 1, 2, 3, 4, 5, 6]

arr.forEach( (val) => {
    console.log(val);
    
}
)
//does not create a new array
//does not have return only log
//if wanna create a new array use map
